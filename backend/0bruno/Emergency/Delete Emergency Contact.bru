meta {
  name: Delete Emergency Contact
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/emergency-contacts/{{contact_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

