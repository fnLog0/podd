meta {
  name: Voice Stream
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/voice/stream
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:multipartForm {
  file: {{audio_file}}
}
