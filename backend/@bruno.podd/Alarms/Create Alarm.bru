meta {
  name: Create Alarm
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/alarms
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "time": "08:00",
    "label": "Morning medication",
    "repeat": "daily"
  }
}

vars:pre-request {
  baseUrl: http://127.0.0.1:8000
}
