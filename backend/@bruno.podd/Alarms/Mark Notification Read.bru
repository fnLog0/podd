meta {
  name: Mark Notification Read
  type: http
  seq: 6
}

put {
  url: {{baseUrl}}/api/notifications/{{notification_id}}/read
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
