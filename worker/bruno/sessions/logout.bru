meta {
  name: Logout
  type: http
  seq: 2
}

post {
  url: {{base_url}}/sessions/logout
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data.logged_out: eq true
}

script:post-response {
  const body = res.getBody();
  if (body.success) {
    bru.setEnvVar("token", "");
  }
}
