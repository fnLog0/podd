meta {
  name: Chat
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/chat
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "message": "I ate roti and chawal for lunch",
    "locale": "en-IN",
    "channel": "text"
  }
}

vars:pre-request {
  baseUrl: http://127.0.0.1:8000
}
