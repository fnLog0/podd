meta {
  name: Chat - Voice Channel
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/chat
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "message": "I had breakfast this morning",
    "locale": "en-IN",
    "channel": "voice"
  }
}

vars:pre-request {
  baseUrl: http://127.0.0.1:8000
}
